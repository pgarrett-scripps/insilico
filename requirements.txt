# The scripts that fetch preprints and run the referee panel.
#
# The site is built by Astro (see package.json); nothing here is needed to
# render it. The referee panel itself is pinned per-run by
# .github/workflows/review.yml, which resolves the current HEAD SHA so every
# review records exactly which version produced it. For local use:
#
#     pip install "peerreviewagents[research] @ git+https://github.com/pgarrett-scripps/PeerReviewAgents.git"
#
# fetch_preprint.py and check_updates.py are stdlib-only by design, so the
# staleness check keeps working without any of this installed.

pyyaml>=6.0
