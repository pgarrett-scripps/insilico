.venv/
__pycache__/
*.pyc
reports/
.env
.DS_Store

# Astro build output and tooling
node_modules/
dist/
.astro/

# Review runs made to test a pipeline change rather than to publish.
# --publish is what puts a bundle in docs/reviews/; everything else lands here.
runs/
